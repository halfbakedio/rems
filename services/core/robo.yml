db:
  summary: connect to the database
  exec: psql -U postgres -h 127.0.0.1 -d core_development
  env: ["PGPASSWORD=postgres"]

proto:
  summary: compile protobuf files
  exec: ./script/gen-protos.sh
